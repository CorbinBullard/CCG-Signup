export enum FieldType {
  string = 'string',
  number = 'number',
  boolean = 'boolean',
  date = 'date',
  email = 'email',
  select = 'select',
  composite = 'composite',
}

export enum SubFieldType {
  string = 'string',
  number = 'number',
  boolean = 'boolean',
  date = 'date',
  email = 'email',
  select = 'select',
}
