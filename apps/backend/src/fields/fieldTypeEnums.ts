export enum FieldTypeEnum {
  string = 'string',
  number = 'number',
  boolean = 'boolean',
  date = 'date',
  email = 'email',
  select = 'select',
  composite = 'composite',
  multiResponse = 'multiResponse',
}

export enum SubFieldTypeEnum {
  string = 'string',
  number = 'number',
  boolean = 'boolean',
  date = 'date',
  email = 'email',
  select = 'select',
}
